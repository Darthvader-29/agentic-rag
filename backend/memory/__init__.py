"""Phase 7: 3-layer memory — per-session markdown notes, a networkx knowledge graph, and the
existing Pinecone vectors, merged by a hybrid retriever. All layers are persisted/shared; an
in-process copy is a short-lived cache only (Phase 5 statelessness)."""
